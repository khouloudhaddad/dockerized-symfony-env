version: '3'

services:

  ngnix:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./:/app

  php:
    build: ./
    environment:
      PHP_IDE_CONFIG: "serverName=shop"
    volumes:
      - ./:/app
      - ./xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdbug.ini

  db:
    image: postgres:13.3
    environment:
      POSTGRES_DB: "shop"
      POSTGRES_USER: "khouloud"
      POSTGRES_PASSWORD: "123"
    ports:
      - "5432:5432"
